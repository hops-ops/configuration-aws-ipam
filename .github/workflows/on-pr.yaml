name: on-pr

on:
  pull_request:
    branches:
      - main
    types:
      - labeled
      - opened
      - reopened
      - synchronize
    paths:
      - '.github/workflows/on-pr.yaml'
      - '.gitops/**'
      - 'apis/**'
      - 'examples/**'
      - 'tests/**'
      - 'functions/**'
      - 'upbound.yaml'

permissions:
  packages: write
  contents: write
  issues: write
  pull-requests: write

jobs:
  validate:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/validate.yaml@v2.5.0
    with:
      examples: |
        [
          { "example": "examples/ipams/example-minimal.yaml" },
          { "example": "examples/ipams/example-multi-region.yaml" },
          { "example": "examples/ipams/example-private-ipv6.yaml" },
          { "example": "examples/ipams/example-private-ipv6.yaml", "observed_resources": "examples/observed-resources/example-private-ipv6/steps/1" },
          { "example": "examples/ipams/example-private-ipv6.yaml", "observed_resources": "examples/observed-resources/example-private-ipv6/steps/2" },
          { "example": "examples/ipams/example-private-ipv6.yaml", "observed_resources": "examples/observed-resources/example-private-ipv6/steps/3" },
          { "example": "examples/ipams/example-with-subnet-pool.yaml" },
          { "example": "examples/ipams/example-with-subnet-pool.yaml", "observed_resources": "examples/observed-resources/example-with-subnet-pool/steps/1" },
          { "example": "examples/ipams/example-with-subnet-pool.yaml", "observed_resources": "examples/observed-resources/example-with-subnet-pool/steps/2" }
        ]
      api_path: apis/ipams
      error_on_missing_schemas: true

  test:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/test.yaml@v2.5.0

  e2e:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/e2e.yaml@v2.5.0
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    with:
      aws: true

  publish:
    needs:
      - validate
      - test
    uses: unbounded-tech/workflows-crossplane/.github/workflows/publish.yaml@v2.5.0
    secrets: inherit
    with:
      tag: pr-${{ github.event.pull_request.number }}-${{ github.sha }}
